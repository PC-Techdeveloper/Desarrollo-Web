# Importando desde un paquete

# Importando los modulos frac y stats
from extramath import frac, stats

# Usando la función 'gcd()' del módulo frac
print(frac.gcd(21, 35))

# Usando la función 'mean()' del módulo stats del paquete 'extramath'
print(stats.mean(6, 3, 9, 5))
